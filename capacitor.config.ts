import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.daniromo.qbitmc',
  appName: 'qbitmc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
